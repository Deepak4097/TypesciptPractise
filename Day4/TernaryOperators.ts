//Ternary operators or conditional operator ? :
//syntax exp1 ? res1 :res2

let a :number =100 ,b:number=200

let res:number=(a>b)? a : b //if the (a>b) is return true  a value store in res if false b value store in res
console.log(res) //100>200 stmt false so 200